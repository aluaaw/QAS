<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qas.web.mappers.UserMapper">
    <insert id="insertUser" parameterType="com.qas.web.domains.User">
        insert into user (
        user_idx, id, password, create_date, update_date)
            values(#{id}, #{password}, now(), now())
    </insert>

    <update id="updateUser" parameterType="com.qas.web.domains.User">
        update user set
        updateDate as update_date = now()
        where user_idx like #{userIdx}
    </update>

    <delete id="deleteUser" parameterType="com.qas.web.domains.User">
        delete from user
        where user_idx like #{userIdx}
    </delete>
</mapper>